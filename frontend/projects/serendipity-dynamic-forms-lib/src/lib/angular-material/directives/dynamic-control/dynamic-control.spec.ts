import { DynamicControl } from './dynamic-control';

describe('DynamicControl', () => {
  it('should create an instance', () => {
    const directive = new DynamicControl();
    expect(directive).toBeTruthy();
  });
});
